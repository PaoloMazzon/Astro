/// \file WrenPreprocessor.h
/// \author Paolo Mazzon
/// \brief Modifies Wren source files before passing them to the VM
#pragma once

// Returns a
const char *vksk_PreprocessSource(const char *filename);